# Copy to .env and fill in. .env is gitignored — never commit real credentials.

# MongoDB connection string (Atlas SRV URI or mongodb://localhost:27017/finshield)
MONGODB_URL=mongodb://localhost:27017/finshield

# Secret used to sign auth JWTs
JWT_SECRET=change-me

PORT=4000
